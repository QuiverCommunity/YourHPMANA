import { Page } from '../types'

import { Dashboard } from '../pages/dashboard'

export const DASHBOARD: Page = {
  key: 'dashboard',
  name: 'Dashboard',
  path: '/dashboard',
  component: Dashboard,
}